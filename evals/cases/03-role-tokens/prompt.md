Add a card component under `app/components/ui/` that shows an invoice summary:
the reference, the amount, and a status badge.

It needs to read correctly in both light and dark mode.

Write the files.
