{
  "name": "pipeline",
  "description": "Lifecycle orchestrator over rails-flow and qa-flow: drives feature build -> QA verify -> certify -> containerized release across stages, honoring every gate. Deploy stage produces a versioned Docker image (ghcr.io) as the release artifact \u2014 pull-and-run verified locally today, Kamal to cloud when ready, same image. Local git-hook nudges detect transitions without spending tokens automatically. Cloud deploy (/pipeline:deploy-cloud) generates Kamal config from a prepared .kamal/deploy.env, wiring secrets safely (names in committed deploy.yml, values only in gitignored .kamal/secrets).",
  "version": "1.3.2",
  "author": {
    "name": "Fisayo Animashaun"
  }
}
