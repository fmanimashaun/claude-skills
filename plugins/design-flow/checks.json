{
  "plugin": "design-flow",
  "comment": [
    "Checks design-flow can run against a USER'S repo. Format documented in rails-flow/checks.json.",
    "rendered_conformance.py is deliberately absent: it needs a browser, which is the user's",
    "dependency. A gate that skips when Playwright is missing reads as a pass, so it stays in the",
    "agent-driven browser pass where its absence is visible.",
    "",
    "brand-pack was broken from the day it was written and nothing here could have caught it: it",
    "named `app/assets/stylesheets/brand`, a path /design-flow:setup never creates (packs live in",
    "`brands/<slug>/`), so it was permanently NOT APPLICABLE -- and it passed no pack directory, so",
    "on the one repo where it did apply it would have exited 2 on a usage error. Not-applicable is",
    "not a pass, which is exactly why project_gates.py says so on every run."
  ],
  "checks": [
    {
      "id": "variant-conformance",
      "why": "Variants that differ in anything but composition are a fork of the design system, and a switcher route left reachable in production ships every rejected screen.",
      "command": [
        "python3",
        "{plugin}/scripts/variant_conformance.py",
        "."
      ],
      "applies_when": [
        "app/views/design_variants"
      ],
      "requires": [
        "python3"
      ]
    },
    {
      "id": "token-drift",
      "why": "A pack moves upstream and the project stands still — drift enters with NO local edit, so no change-triggered mechanism can catch it. Only an unconditional check can.",
      "command": [
        "python3",
        "{plugin}/scripts/check_token_drift.py"
      ],
      "applies_when": [
        "app/assets/tailwind/application.css"
      ],
      "requires": [
        "python3"
      ]
    }
  ]
}
